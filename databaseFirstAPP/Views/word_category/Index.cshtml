@model IEnumerable<databaseFirstAPP.word_category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.category.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.word.word1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.category.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.word.word1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.word_category_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.word_category_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.word_category_ID })
        </td>
    </tr>
}

</table>
